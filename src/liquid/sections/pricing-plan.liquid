<!-- pricing-plan.liquid -->
<div class="{% if section.settings.fullwidth %}container-fluid{% else %}container{% endif %}">
  <div class="section-block">
    {%- include 'section-heading' -%}
    <div class="dots-negative">
      <div class="row" id="pricing-list-{{ section.id }}"
           data-rows="{{section.settings.rows}}"
           data-slidesToShow="{{section.blocks.size}}"
           data-slidestoscroll="{{section.blocks.size}}"
           data-arrows="{{ section.settings.arrows }}"
           data-dots="{{ section.settings.dots }}" 
           data-draggable="{{ section.settings.draggable }}" 
           data-infinite="{{ section.settings.infinite }}" 
           data-section-id="{{ section.id }}" 
           data-section-type="slickCarousels">
        {%- for block in section.blocks -%}
        <div class="col carousel__slide-wrapper--{{block.id}}" {{ block.shopify_attributes }}>
          <div class="py-5 px-3 px-lg-5 transition text-center pricing-plan--1 {% if block.settings.special %}pricing-plan--special bg-theme gradient-theme text-white{% else %}bg-theme-50{% endif %}">
            <p class="plan-title text-uppercase font-family-2 fz-2 m-0">{{block.settings.title}}</p>
            <p class="plan-price font-family-2">{{block.settings.price}}</p>
            <div class="plan-info pb-5">{{block.settings.des}}</div>
            <a href="{{block.settings.btn_link}}" class="plan-link position-relative btn w-50 py-3 btn-theme gradient-theme mt-lg-5 rounded-pill fz-n4 font-weight-bold">{{block.settings.btn_text}}</a>
          </div>
        </div>
        {%- endfor -%}
      </div>
    </div>
    {%- if section.blocks.size == 0 -%}
    {%- include 'no-blocks' -%}
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Pricing plans",
  "class": "index-section",
  "max_blocks": 6,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Pricing plans"
    },
    {
      "type": "textarea",
      "id": "des",
      "label": "HTML / Description"
    },
    {
      "type": "checkbox",
      "id": "fullwidth",
      "label": "Full width",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "arrows",
      "label": "Arrows",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "draggable",
      "label": "Draggable",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "dots",
      "label": "Dots",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "infinite",
      "label": "Infinite",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "Plan",
      "name": "Plan",
      "settings": [
        {
          "id": "special",
          "type": "checkbox",
          "label": "Special plan",
          "default": false
        },
        {
          "id": "title",
          "type": "text",
          "label": "Title",
          "default": "Plan heading"
        },
        {
          "id": "price",
          "type": "text",
          "label": "Price",
          "default": "$399"
        },
        {
          "id": "des",
          "type": "textarea",
          "label": "Information"
        },
        {
          "id": "btn_text",
          "type": "text",
          "label": "Button text",
          "default": "View more"
        },
        {
          "id": "btn_link",
          "type": "url",
          "label": "Button link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Pricing Plans",
      "category": "Text",
      "blocks": [
        {
          "type": "Plan"
        },
        {
          "type": "Plan"
        },
        {
          "type": "Plan"
        }
      ]
    }
  ]
}
{% endschema %}